import httpServiceInit, { apiRoutes, PREFIX_API } from "../utils/HttpService";

const httpService = httpServiceInit();



export async function uploadFile(
    file, fileName, encryption, keyMaster
) : Promise<UploadOutpu>{
    
}
